### React Auth Provider Router (in progress)

- Will use React Router
- Typescript
- more to come...

#### To use

- `yarn install`
- `yarn bootstrap`
- `yarn start`

#### Run in Docker

`docker compose up`
